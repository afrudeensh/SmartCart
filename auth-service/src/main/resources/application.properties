# Database
spring.datasource.url=jdbc:mysql://localhost:3306/smartcart_auth_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=AFRUafru15301#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8081

# JWT
jwt.secret=smartcart-secret-key-must-be-at-least-256-bits-long-for-hs256
# 15 minutes
jwt.access-token-expiration=900000       
# 7 days
jwt.refresh-token-expiration=604800000 

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.try-it-out-enabled=true

# ── Mail (Gmail SMTP) ─────────────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=afrudeenawsjavaai@gmail.com
spring.mail.password=AFRUafru15301#
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ── App config ────────────────────────────────────────────────────────────────
app.frontend-url=http://localhost:4200
app.cors.allowed-origins=http://localhost:4200
app.password-reset.expiry-minutes=15
app.kafka.enabled=false
app.outbox.poll-interval-ms=5000